#include <iostream>
#include <string>
#include <vector>

using namespace std;

string initialiseGameBoard(string gameBoard);
void displayBoard(string currentboard);
string playerChoice(string currentBoard);
string computerChoice(string currentBoard);
char outCome(string currentBoard);